#!/usr/bin/env node
'use strict';
const fs=require('fs');const path=require('path');
const ROOT=path.resolve(__dirname,'..'),DATA=path.join(ROOT,'data');
const inputArg=process.argv.slice(2).find(x=>!x.startsWith('--'));const dry=process.argv.includes('--dry-run'),write=process.argv.includes('--write');
const fail=m=>{console.error(`INGESTION FAILED: ${m}`);process.exit(1)};
if(!inputArg)fail('usage: node tools/ingest-snapshot-v2.js <snapshot.json> [--dry-run|--write]');if(dry===write)fail('choose exactly one mode');
const read=f=>{try{return JSON.parse(fs.readFileSync(f,'utf8'))}catch(e){fail(`cannot read ${path.relative(ROOT,f)}: ${e.message}`)}};
const save=(f,v)=>{fs.mkdirSync(path.dirname(f),{recursive:true});fs.writeFileSync(f,JSON.stringify(v,null,2)+'\n','utf8')};
const nn=(v,f)=>{if(!Number.isInteger(v)||v<0)fail(`${f} must be a non-negative integer`)};const req=(v,f)=>{if(v===undefined||v===null||v==='')fail(`${f} is required`)};
const esc=v=>String(v).replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;').replace(/'/g,'&#39;');
const move=v=>v===0?'-':v>0?`↑ ${v}`:`↓ ${Math.abs(v)}`;
const weapons=w=>[['25mm',w['25mm']],['hydra',w.hydra],['hellfire',w.hellfire]].map(([k,n])=>w.upgrade_deltas?.[k]?`${n} (+${w.upgrade_deltas[k]})`:String(n)).join(' / ');
const input=read(path.resolve(inputArg));const playersFile=read(path.join(DATA,'players.json'));const snapshots=read(path.join(DATA,'snapshots.json'));const history=read(path.join(DATA,'player-observations-history.json'));const current=read(path.join(DATA,'player-observations.json'));const memberships=read(path.join(DATA,'memberships.json'));const index=read(path.join(DATA,'index.json'));
if(!input.snapshot||!Array.isArray(input.players))fail('input must contain snapshot and players[]');const s=input.snapshot;['captured_at_utc','date_persian','time_iran','type','members','capacity'].forEach(f=>req(s[f],`snapshot.${f}`));nn(s.members,'snapshot.members');nn(s.capacity,'snapshot.capacity');if(s.members!==input.players.length)fail('snapshot.members does not equal players.length');if(s.members>s.capacity)fail('members exceed capacity');
const existing=snapshots.snapshots||[];if(existing.some(x=>x.captured_at_utc===s.captured_at_utc))fail('snapshot timestamp already exists');const ids=new Set(existing.map(x=>x.snapshot_id));const seq=Math.max(0,...existing.map(x=>Number(String(x.snapshot_id).replace(/^S/,''))||0))+1;const sid=s.snapshot_id||`S${String(seq).padStart(2,'0')}`;if(ids.has(sid))fail(`${sid} already exists`);
const byId=new Map(playersFile.players.map(p=>[p.player_id,p])),byName=new Map();playersFile.players.forEach(p=>byName.set(p.display_name,[...(byName.get(p.display_name)||[]),p]));let nextNo=Math.max(0,...playersFile.players.map(p=>Number(String(p.player_id).replace('PERSIA-P-',''))||0));const seenIds=new Set(),seenRanks=new Set(),rows=[];
for(const src of input.players){req(src.display_name,'player.display_name');if(!Number.isInteger(src.rank)||src.rank<1)fail(`${src.display_name}: invalid rank`);if(seenRanks.has(src.rank))fail(`${sid}: duplicate rank ${src.rank}`);seenRanks.add(src.rank);let p=src.player_id?byId.get(src.player_id):null;if(src.player_id&&!p)fail(`${src.display_name}: unknown player_id`);if(!p){const matches=byName.get(src.display_name)||[];if(matches.length>1)fail(`ambiguous identity: ${src.display_name}; supply player_id`);if(matches.length===1)p=matches[0]}
if(p&&p.display_name!==src.display_name)fail(`identity mismatch: ${p.player_id} is ${p.display_name}, input says ${src.display_name}`);if(!p){if(src.confirmed_new_identity!==true)fail(`new identity ${src.display_name} requires confirmed_new_identity=true`);nextNo++;p={player_id:`PERSIA-P-${String(nextNo).padStart(4,'0')}`,display_name:src.display_name,status:'active',role:src.role||'Member',first_seen_snapshot:sid,last_seen_snapshot:sid};byId.set(p.player_id,p);byName.set(p.display_name,[p])}
if(seenIds.has(p.player_id))fail(`${sid}: duplicate player ${p.player_id}`);seenIds.add(p.player_id);['stage','league_medals','league_medals_delta','clan_medals','total_kills','kills_delta'].forEach(f=>nn(src[f],`${src.display_name}.${f}`));['gold','silver','bronze'].forEach(f=>nn(src.honor_medals?.[f],`${src.display_name}.honor_medals.${f}`));['25mm','hydra','hellfire'].forEach(f=>nn(src.weapons?.[f],`${src.display_name}.weapons.${f}`));req(src.last_online_display,`${src.display_name}.last_online_display`);rows.push({player_id:p.player_id,display_name:p.display_name,role:src.role||p.role||'Member',rank:src.rank,rank_movement:src.rank_movement||0,stage:src.stage,league_medals:src.league_medals,league_medals_delta:src.league_medals_delta,clan_medals:src.clan_medals,honor_medals:src.honor_medals,total_kills:src.total_kills,kills_delta:src.kills_delta,weapons:src.weapons,last_online_display:src.last_online_display})}
if(rows.length!==s.members||seenRanks.size!==s.members||seenIds.size!==s.members)fail(`${sid}: incomplete normalized coverage`);
const previous=[...existing].sort((a,b)=>a.captured_at_utc.localeCompare(b.captured_at_utc)).at(-1)||null;const previousRows=previous?(history.snapshots?.[previous.snapshot_id]||[]):[];const oldSet=new Set(previousRows.map(x=>x.player_id)),newSet=new Set(rows.map(x=>x.player_id));const added=[...newSet].filter(x=>!oldSet.has(x)),removed=[...oldSet].filter(x=>!newSet.has(x)),retained=newSet.size-added.length;
const meta={snapshot_id:sid,captured_at_utc:s.captured_at_utc,date_persian:s.date_persian,time_iran:s.time_iran,type:s.type,members:s.members,capacity:s.capacity,source_report:s.source_report||`/clan-leaderboard/reports/${s.report_filename||`${s.captured_at_utc.slice(0,10)}-${s.time_iran.replace(':','')}.html`}`,normalized_observations:true,observation_source:'player-observations-history.json'};
const nextPlayers={version:playersFile.version,id_prefix:playersFile.id_prefix,players:playersFile.players.map(p=>{const r=rows.find(x=>x.player_id===p.player_id);return r?{...p,display_name:r.display_name,role:r.role,last_seen_snapshot:sid}:p}).concat(rows.filter(r=>!playersFile.players.some(p=>p.player_id===r.player_id)).map(r=>({player_id:r.player_id,display_name:r.display_name,status:'active',role:r.role,first_seen_snapshot:sid,last_seen_snapshot:sid})))};
const nextMemberships=JSON.parse(JSON.stringify(memberships));const mem=nextMemberships.memberships||[];for(const m of mem)if(m.status==='active'&&newSet.has(m.player_id))m.through_snapshot=sid;for(const id of added){mem.push({player_id:id,from_snapshot:sid,through_snapshot:sid,status:'active',start_event:previous?`joined_between_${previous.snapshot_id}_and_${sid}`:'joined_with_initial_snapshot'})}for(const id of removed){const m=mem.find(x=>x.status==='active'&&x.player_id===id);if(m){m.through_snapshot=previous?.snapshot_id||m.through_snapshot;m.status='ended';m.end_precision='between_snapshots';m.end_event='left_or_kicked'}}nextMemberships.memberships=mem;
const nextSnapshots={...snapshots,current_snapshot_id:sid,snapshots:[...existing,meta]};const nextHistory={...history,snapshots:{...(history.snapshots||{}),[sid]:rows}};const nextCurrent={...current,snapshots:{[sid]:rows}};
const reportFilename=s.report_filename||`${s.captured_at_utc.slice(0,10)}-${s.time_iran.replace(':','')}.html`;const total=rows.reduce((a,r)=>a+r.clan_medals,0);const topKills=[...rows].sort((a,b)=>b.kills_delta-a.kills_delta).slice(0,5).map(r=>`${r.display_name} +${r.kills_delta.toLocaleString('en-US')}`).join(' · ');const topLeague=[...rows].sort((a,b)=>b.league_medals_delta-a.league_medals_delta).slice(0,5).map(r=>`${r.display_name} +${r.league_medals_delta.toLocaleString('en-US')}`).join(' · ');const body=rows.map(r=>`<tr><td>${r.rank} (${move(r.rank_movement)})</td><td>${esc(r.display_name)}</td><td>${esc(r.role)}</td><td>${r.stage}</td><td>${r.league_medals.toLocaleString('en-US')}</td><td>${r.league_medals_delta?'+':''}${r.league_medals_delta.toLocaleString('en-US')}</td><td>${r.clan_medals.toLocaleString('en-US')}</td><td>${r.honor_medals.gold} / ${r.honor_medals.silver} / ${r.honor_medals.bronze}</td><td>${r.total_kills.toLocaleString('en-US')}</td><td>${r.kills_delta?'+':''}${r.kills_delta.toLocaleString('en-US')}</td><td>${weapons(r.weapons)}</td><td>${esc(r.last_online_display)}</td></tr>`).join('\n');
const report=`<!doctype html>\n<html lang="fa" dir="rtl" data-theme="dark">\n<head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover"><title>PERSIA · Clan Leaderboard · ${esc(s.date_persian)} · ${esc(s.time_iran)}</title><link rel="stylesheet" href="../../assets/css/app.css"><link rel="stylesheet" href="../../assets/css/site.css"></head><body><header class="topbar"><div class="shell"><a class="brand" href="../../index.html"><span class="brand-mark">◆</span><span><b>PERSIA</b> · War Drone</span></a></div></header><main><section class="hero"><div class="shell"><span class="badge">PERSIA · ${esc(s.season_label||'CLAN')}</span><h1>جدول جامع عملکرد و تغییرات اعضای کلن</h1><p>${esc(s.date_persian)} · ${esc(s.time_iran)} · تمام ${s.members} عضو بررسی و ثبت شده‌اند.</p></div></section><section class="section"><div class="shell"><h2>وضعیت کلی و شاخص‌های کلیدی کلن</h2><p>ظرفیت اعضا: ${s.members} از ${s.capacity} نفر · Snapshot قبلی: ${previous?.snapshot_id||'—'}</p><ul><li>اعضای مشترک با Snapshot قبلی: ${retained}</li><li>بازیکنان حذف‌شده: ${removed.length} · بازیکنان اضافه‌شده: ${added.length}</li><li>مدال کل کلن: ${total.toLocaleString('en-US')}</li><li>بیشترین افزایش کیل: ${esc(topKills)}</li><li>بیشترین افزایش مدال لیگ جاری: ${esc(topLeague)}</li></ul><h2>جدول کامل رتبه‌بندی و تغییرات عملکرد اعضا</h2><div class="table-wrap"><table><thead><tr><th>رتبه</th><th>نام کاربری</th><th>سمت</th><th>استیج</th><th>مدال لیگ جاری</th><th>تغییر مدال لیگ</th><th>مدال کل کلن</th><th>مدال افتخار (طلا / نقره / برنز)</th><th>مجموع کیل 💀</th><th>افزایش کیل 💀</th><th>لول سلاح‌ها (توپ / هیدرا / هل‌فایر)</th><th>آخرین آنلاین</th></tr></thead><tbody>${body}</tbody></table></div></div></section></main></body></html>\n`;
const oldReports=index.reports||[];const nextIndex={...index,reports:[{id:reportFilename.replace(/\.html$/,''),period:oldReports.length?Math.max(...oldReports.map(r=>Number(r.period)||0))+1:1,date_persian:s.date_persian,time:s.time_iran,type:s.type,members:s.members,capacity:`${s.members}/${s.capacity}`,page:`../reports/${reportFilename}`},...oldReports]};
const result={ok:true,mode:dry?'dry-run':'write',snapshot_id:sid,members:s.members,added,removed,retained,total_clan_medals:total,report:`reports/${reportFilename}`};console.log(JSON.stringify(result,null,2));
if(write){save(path.join(DATA,'players.json'),nextPlayers);save(path.join(DATA,'snapshots.json'),nextSnapshots);save(path.join(DATA,'player-observations.json'),nextCurrent);save(path.join(DATA,'player-observations-history.json'),nextHistory);save(path.join(DATA,'memberships.json'),nextMemberships);save(path.join(DATA,'index.json'),nextIndex);fs.mkdirSync(path.join(ROOT,'reports'),{recursive:true});fs.writeFileSync(path.join(ROOT,'reports',reportFilename),report,'utf8');console.log('WRITE COMPLETE')}
