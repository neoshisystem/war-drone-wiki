(()=>{'use strict';document.querySelectorAll('.faq-q').forEach(btn=>btn.addEventListener('click',()=>{const card=btn.closest('.faq-card');card.classList.toggle('open');btn.setAttribute('aria-expanded',card.classList.contains('open')?'true':'false');}));const stages={5:{kills:57,time:80},14:{kills:100,time:210},19:{kills:95,time:180}};const stage=document.querySelector('#calcStage'),runs=document.querySelector('#calcRuns'),out=document.querySelector('#calcResult'),meta=document.querySelector('#calcMeta');function render(){if(!stage||!runs||!out)return;const s=stages[Number(stage.value)]||stages[5],r=Math.max(1,Math.min(999,Number(runs.value)||1));const kills=s.kills*r,seconds=s.time*r;const ads=Math.max(0,Math.ceil(Math.max(0,r-10)/2));const total=seconds+(ads*30);const m=Math.floor(total/60),sec=total%60;out.textContent=kills.toLocaleString('fa-IR')+' کیل';meta.textContent=`زمان پایه: ${Math.floor(seconds/60)}:${String(seconds%60).padStart(2,'0')} | تبلیغ لازم پس از ۱۰ انرژی اولیه: ${ads} | زمان با تبلیغ: ${m}:${String(sec).padStart(2,'0')}`;}stage?.addEventListener('change',render);runs?.addEventListener('input',render);render();})();